import React from 'react';
import ResumeImg from './images/resume.png';

const Resume = (props) => {
  return <img src={ResumeImg} className='resume-image' alt='Resume' />;
};

export default Resume;
